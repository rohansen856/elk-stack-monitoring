metricbeat.config.modules:
  path: ${path.config}/modules.d/*.yml
  reload.enabled: false
  enabled: false

metricbeat.modules:
# System metrics for APT detection
- module: system
  metricsets:
    - cpu
    - load
    - memory
    - network
    - filesystem
    - fsstat
  enabled: true
  period: 30s
  cpu.metrics:  [percentages]
  core.metrics: [percentages]

# Docker metrics - disabled due to socket access issues in container
- module: docker
  metricsets:
    - container
    - cpu
    - diskio
    - healthcheck
    - info
    - memory
    - network
  hosts: ["unix:///var/run/docker.sock"]
  period: 10s
  enabled: false

# Network monitoring for suspicious connections - disabled due to field complexity
- module: system
  metricsets: ["socket"]
  socket.reverse_lookup.enabled: true
  socket.reverse_lookup.success_ttl: 60s
  socket.reverse_lookup.failure_ttl: 60s
  period: 30s
  enabled: false

processors:
- add_host_metadata:
    when.not.contains.tags: forwarded
- add_docker_metadata: ~

output.elasticsearch:
  hosts: ["elasticsearch:9200"]
  index: "metricbeat-%{[agent.version]}-%{+yyyy.MM.dd}"

setup.kibana:
  host: "kibana:5601"

setup.dashboards.enabled: false
setup.template.enabled: true
setup.template.name: "metricbeat"
setup.template.pattern: "metricbeat-*"
setup.template.settings:
  index.number_of_shards: 1
  index.number_of_replicas: 0

logging.level: info